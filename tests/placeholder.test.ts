// import { PromisePool } from '@supercharge/promise-pool';

test('placeholder', async () => {
    // const box: Promise<unknown>[] = [];
    // for (let i = 0; i < 10; i++) {
    //     box.push(fetch('https://dog.ceo/api/breeds/image/random')); //esling-disable-line
    // }
    // const { results, errors } = await PromisePool.for(box).process(async (data, i, pool) => {
    //     return await data;
    // });
    // console.log(results);

    expect(1).toEqual(1);
});
